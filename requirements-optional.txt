# Optional providers — install only what you need

# STT: Deepgram (cloud, lower latency than faster-whisper)
deepgram-sdk>=3.5.0

# TTS: Kokoro ONNX (local, higher quality voice than Piper)
kokoro-onnx>=0.4.0
